name: Companion website
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true
jobs:
  site:
    uses: johntfoster/research-agent-workflows/.github/workflows/research-site.yml@__SHARED_REVISION__
    with:
      build-argv: '["python3", ".agent/shared/tools/research_project.py", "site"]'
      deploy: ${{ github.ref == 'refs/heads/main' && github.event_name != 'pull_request' }}
